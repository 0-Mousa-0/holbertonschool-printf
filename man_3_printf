.\" Manpage for _printf
.\" Description: printf function implementation
.\" Author: [Raghad Almalki / Mousa Alq¶arni]

.TH _PRINTF 3 "2025" "1.0" "Custom Printf Manual"

.SH NAME
_printf \- formatted output conversion

.SH SYNOPSIS
.nf
.B #include "main.h"
.PP
.BI "int _printf(const char *" format ", ...);"
.fi

.SH DESCRIPTION
The
.B _printf
function produces output according to a
.I format
string and writes it to
.B stdout,
the standard output stream.

The
.I format
string is a character string composed of zero or more directives:
.IP \(bu 2
Ordinary characters (not
.BR % ),
which are copied unchanged to the output stream.
.IP \(bu 2
Conversion specifications, each of which results in fetching zero or more
subsequent arguments. Each conversion specification is introduced by the
.B %
character and ends with a conversion specifier.

.SH CONVERSION SPECIFIERS
The following conversion specifiers are supported:

.TP
.B %c
Prints a single character.

.TP
.B %s
Prints a string of characters.

.TP
.B %%
Prints a literal % character.

.TP
.B %d, %i
Prints a signed decimal integer.

.PP
The
.B _printf
function does not handle flag characters, field width, precision, or length modifiers.

.SH RETURN VALUE
Upon successful return, the
.B _printf
function returns the number of characters printed (excluding the null byte used to end output to strings).

If an output error is encountered, a negative value is returned.

.SH EXAMPLES
The following example demonstrates the use of
.BR _printf :

.nf
#include "main.h"

int main(void)
{
    int len;
    
    len = _printf("Hello, %s!\\n", "world");
    _printf("Printed %d characters\\n", len);
    _printf("Character: %c, Number: %d\\n", 'A', 42);
    _printf("Percent sign: %%\\n");
    
    return (0);
}
.fi

.SH COMPILATION
Compile your code with:
.nf
.B gcc -Wall -Werror -Wextra -pedantic -std=gnu89 -Wno-format *.c
.fi

.SH AUTHORED FUNCTIONS AND MACROS
The implementation of
.B _printf
may use the following authorized functions:
.IP \(bu 2
.BR write (2)
.IP \(bu 2
.BR malloc (3)
.IP \(bu 2
.BR free (3)
.IP \(bu 2
.BR va_start (3)
.IP \(bu 2
.BR va_end (3)
.IP \(bu 2
.BR va_copy (3)
.IP \(bu 2
.BR va_arg (3)

.SH BUGS
The
.B _printf
function is a custom implementation and may not handle all edge cases exactly like the standard
.BR printf (3)
function.

This implementation does not handle buffer management like the C library printf function.

.SH SEE ALSO
.BR printf (3),
.BR write (2),
.BR stdarg (3)

.SH STANDARDS
This implementation follows the GNU89 standard.

.SH HISTORY
